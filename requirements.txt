# ============================================================
# Banco Futura – Churn & VIP Detection System
# requirements.txt | Enterprise Production Dependencies
# ============================================================

# ── Core PySpark / Databricks ────────────────────────────────
pyspark==3.5.1
delta-spark==3.1.0
databricks-sdk==0.24.0
databricks-connect==15.1.0

# ── Machine Learning ─────────────────────────────────────────
scikit-learn==1.4.2
xgboost==2.0.3
lightgbm==4.3.0
imbalanced-learn==0.12.2          # SMOTE para clases desbalanceadas
optuna==3.6.1                     # Hyperparameter tuning bayesiano
shap==0.45.0                      # Explainability (SHAP values)

# ── NLP / Embeddings ─────────────────────────────────────────
transformers==4.40.1
sentence-transformers==2.7.0
torch==2.3.0
tokenizers==0.19.1

# ── Data Processing ──────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==16.0.0                   # Formato columnar para Delta Lake
fastparquet==2024.2.0

# ── Feature Engineering ──────────────────────────────────────
feature-engine==1.6.2
category-encoders==2.6.3

# ── APIs ─────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
httpx==0.27.0                     # Cliente async para tests de API

# ── Streaming / Mensajería ───────────────────────────────────
kafka-python==2.0.2
confluent-kafka==2.4.0

# ── Azure ────────────────────────────────────────────────────
azure-storage-blob==12.20.0
azure-identity==1.16.0
azure-keyvault-secrets==4.8.0
azure-eventhub==5.11.7

# ── Observabilidad / Logging ─────────────────────────────────
mlflow==2.13.0                    # Experiment tracking
loguru==0.7.2
opentelemetry-sdk==1.24.0
prometheus-client==0.20.0

# ── Visualización ────────────────────────────────────────────
matplotlib==3.8.4
seaborn==0.13.2
plotly==5.22.0

# ── Testing ──────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0
pytest-asyncio==0.23.6
great-expectations==0.18.15       # Data quality / validación

# ── DevOps / Config ──────────────────────────────────────────
python-dotenv==1.0.1
pyyaml==6.0.1
typer==0.12.3                     # CLI tools
rich==13.7.1                      # Terminal output enriquecido
